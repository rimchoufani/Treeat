# Copy to .env (gitignored) and fill in. On Railway, set these as Variables instead.
INFRARED_API_KEY=your_key_here
# Comma-separated frontend URLs allowed to call this API. Never "*".
ALLOWED_ORIGINS=http://localhost:5173
